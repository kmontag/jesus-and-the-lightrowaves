Required libraries (install them to the Processing `libraries/` folder):
- https://github.com/djrkohler/punktiert

The project also needs to be built before running:

```bash
$ brew install gradle # if you don't have it already
$ gradle build
```